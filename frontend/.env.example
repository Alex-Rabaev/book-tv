# Frontend environment (Vite exposes only VITE_-prefixed vars to the client).
# Base URL of the book-tv backend API. The frontend talks ONLY to this backend (AD-2).
VITE_API_BASE_URL=http://localhost:8000
